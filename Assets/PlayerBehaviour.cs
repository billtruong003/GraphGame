using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using PrimeTween;
public class PlayerBehaviour : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
    private void move()
    {
        // Tween.PositionAtSpeed(transform, MainManager.Instance.pickedPoint.position, 3f, Ease.OutElastic, 1, CycleMode.Restart, 0, 0, false);
    }
}
